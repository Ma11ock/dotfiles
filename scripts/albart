#/usr/bin/env zsh

OIFS="$IFS"
IFS=$'\n'
for i in `find $1 -type f -name "*.mp3"`; do
    file=`echo "$i" | sed 's/ /\\ /g'`
    ffmpeg -i $file -i $2 -b 192k -map_metadata 0 -map 0 -map 1 output.mp3
    mv output.mp3 $file
    exit
done
